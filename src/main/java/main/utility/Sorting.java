package main.utility;

public class Sorting {

    public static void sort(int[] nopol) {
        if (nopol.length > 50) {
            // quick sort
        } else {
            // insertion sort
        }
    }
}
