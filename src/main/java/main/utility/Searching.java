package main.utility;

public class Searching {

    public static void sort(int[] nopol) {
        if (nopol.length > 50) {
            // binary search
        } else {
            // linear search
        }
    }
}
